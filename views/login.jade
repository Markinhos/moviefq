extends layout

block content
    h1 = title
    p Please register
        form.well.span6(role="form", action="/login", method="POST")
            div.form-group
                label(for="userName") User Name
                input#userName.form-control(type="text", name="username", placeholder="User name")
                label(for="password") Password
                input#password.form-control(type="password", name="password", placeholder="Enter password") 

            button.btn.btn-default(type="submit") Submit           
            hr
            div.btm-links
                a.pull-left(href='/signup') Create an account
                a.pull-right(href='/auth/facebook')
                    i.fa.fa-facebook.pull-left
                    p.pull-right Log in with facebook